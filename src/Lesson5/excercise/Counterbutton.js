import React from "react";

function Counterbutton({handleDecrease}, {handleIncrease}) {
    return (
        <>
            <button style={} onClick={handleIncrease}>+</button>
            <button onClick={handleDecrease}>-</button>
        </>
    );
}

export default Counterbutton;